<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Test Results | MCP Server Tester</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f9fafb;
      color: #333;
      margin: 0;
      padding: 0;
    }

    /* Navbar */
    .navbar {
      background-color: #4CAF50;
      padding: 15px 20px;
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
    }

    .navbar h2 {
      margin: 0;
      font-size: 20px;
    }

    .navbar a {
      color: white;
      text-decoration: none;
      font-weight: 500;
      margin-top: 10px;
    }

    .container {
      max-width: 850px;
      margin: 40px auto;
      background: #fff;
      padding: 30px 20px;
      border-radius: 12px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    }

    h1 {
      color: #4CAF50;
      text-align: center;
      margin-bottom: 30px;
      font-size: 26px;
    }

    .success,
    .error {
      padding: 15px 20px;
      border-radius: 8px;
      margin-bottom: 25px;
      font-size: 16px;
    }

    .success {
      background: #e8f5e9;
      color: #2e7d32;
      border-left: 5px solid #43a047;
    }

    .error {
      background: #ffebee;
      color: #c62828;
      border-left: 5px solid #e53935;
    }

    pre {
      background: #f4f4f4;
      padding: 20px;
      border-radius: 8px;
      font-size: 14px;
      overflow-x: auto;
      white-space: pre-wrap;
      word-break: break-word;
      line-height: 1.5;
    }

    .back-btn {
      display: inline-block;
      margin-top: 30px;
      text-decoration: none;
      color: #fff;
      background-color: #4CAF50;
      padding: 10px 24px;
      border-radius: 6px;
      transition: background 0.3s ease;
    }

    .back-btn:hover {
      background-color: #388e3c;
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 20px 10px;
      background-color: #f0f0f0;
      color: #666;
      margin-top: 60px;
      font-size: 14px;
    }

    @media (max-width: 600px) {
      .navbar {
        flex-direction: column;
        align-items: flex-start;
      }

      .navbar a {
        margin-top: 8px;
        margin-left: 0;
      }

      .container {
        margin: 20px 10px;
        padding: 20px 15px;
      }

      h1 {
        font-size: 22px;
      }

      .back-btn {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>

<body>

  <!-- Navbar -->
  <div class="navbar">
    <h2>MCP Server Tester</h2>
    <a href="/">Home</a>
  </div>

  <!-- Main Content -->
  <div class="container">
    <h1>MCP Server Test Results</h1>

    <% if (success) { %>
      <div class="success">
        Server is working correctly!
      </div>
      <pre><%= JSON.stringify(data, null, 2) %></pre>
      <% } else { %>
        <div class="error">
          Server failed the test.
        </div>
        <% } %>

          <a href="/" class="back-btn">Back to Home</a>
  </div>

  <!-- Footer -->
  <footer>
    © <%= new Date().getFullYear() %> MCP Tester Built by — Nand Kumar Sahu
  </footer>

</body>

</html>